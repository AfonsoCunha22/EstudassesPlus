package com.example.cmprojeto.callbacks;

import com.example.cmprojeto.model.Session;

import java.util.List;

public interface SessionCallback {
    void manageUserSession(List<Session> sessions);
}
